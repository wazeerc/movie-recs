const MovieRecs: React.FC = () => {
  const pageTitle = "Movie Recs";

  return (
    <main>
      <div className="container">
        <h1>{pageTitle}</h1>
      </div>
    </main>
  );
};

export default MovieRecs;
